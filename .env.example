# Required
VAULT_PATH=/Users/yourname/Documents/LifeOs

# Ollama models (swappable — must be pulled first via `ollama pull <model>`)
OLLAMA_MODEL=llama3.1:8b
OLLAMA_EMBED_MODEL=nomic-embed-text

# RAG settings
RAG_TOP_K=5
CHUNK_SIZE=500
CHUNK_OVERLAP=50
